# Online chat row — copy into `../online-chats.md`

| session_id | member | tool | share_url | verified_incognito | notes |
|------------|--------|------|-----------|:------------------:|-------|
| YYYYMMDD-\<TaskID\>-\<member\>-\<tool\>-NN | \<Member\> | Claude.ai / ChatGPT / … | https://…/share/… | ✅ / ❌ | \<ghi chú ngắn; không dán transcript\> |

## Rules (unchanged policy)

- Use public share/export URL only — never paste full chat content into the repo.
- Mark `verified_incognito` only after opening the link in a private window.
- Same `session_id` as the matching `manifest.csv` row when both apply.
