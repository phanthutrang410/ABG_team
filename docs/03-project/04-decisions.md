# Quyết định

1. **ML vs LLM** — Model ghi mức độ ưu tiên; LLM chỉ giải thích, không bịa xác suất.
2. **Privacy** — Chỉ tín hiệu không xâm phạm (xem [Ethics](../02-product/05-ethics.md)).
3. **Care** — Handoff cho người; không workflow phạt tự động.
4. **Fairness** — Metric nhóm là MVP, không phải nice-to-have.
5. **Stack** — FastAPI + Next.js; lấy *ý tưởng* từ EduInsight local, không vendor cả repo cũ vào bài nộp.
6. **LLM primary (lịch sử; provider target đã supersede bởi #22)** — FPT AI Inference (mkp-api.fptcloud.com, OpenAI SDK + đổi base_url). Backup: OpenAI/Gemini. Agent tool-calling ưu tiên Qwen/Qwen3-32B. Deploy BE trên AWS.
7. **Primary system user** — Ban Lãnh đạo Khoa/Trường rà soát và phê duyệt chuyển tiếp; GVCN/cố vấn nhận case đã duyệt và thực hiện chăm sóc.
8. **Product language** — Score chỉ là giá trị nội bộ; UI dùng “mức độ ưu tiên rà soát” và trạng thái case, không gắn nhãn sinh viên.
9. **48h signal scope** — MVP dùng **điểm theo học kỳ** và **điểm danh theo thời gian** (sau source approval + privacy review + contract), kèm coverage và freshness. Không tạo chuỗi chuyên cần giả khi thiếu nguồn — trả `insufficient_data`. LMS/tín chỉ/CLB/thư viện/campus vẫn hậu MVP. Forecasting/gated fusion (M07/M08) là nghiên cứu riêng, không thay thế tín hiệu điểm danh trong MVP.
10. **Brief interpretation** — Các xung đột và khoảng trống được ghi tại [Truy vết yêu cầu](../01-requirements/03-traceability.md); không tự biến đề xuất trong brief thành feature đã cam kết.
11. **D5 AI log** — Thủ công 48h: mỗi người tự ghi manifest.csv + link online; evidence thô (nếu có) nộp Drive, không commit. Không dùng git history thay nhật ký AI. Xem [.ai-log/README.md](../../.ai-log/README.md).
12. **Nguồn EPU** — Dừng dùng/generate synthetic. Chỉ nạp export EPU đã qua source gate (owner, quyền sử dụng, hash, PII exclusion); chuẩn hóa theo [contract EPU](../04-engineering/04-epu-data-integration-contract.md). Thiếu coverage/nhóm audit/mapping thì trả insufficient_data, không bù bằng dữ liệu giả.
13. **Attendance MVP vs hybrid research** — Điểm danh theo thời gian thuộc MVP: Hoàng/`H15` lấy export được data owner phê duyệt và cập nhật contract; ML baseline dùng tín hiệu chuyên cần khi sẵn sàng. `M07`/`H14`/`M08`/`T04` chỉ là nghiên cứu forecasting + gated fusion (ngoài CP2); không thay thế nhánh điểm danh MVP và không gọi output công khai là dropout risk.
14. **Phân công 18/7** — Hoàng là owner duy nhất của docs/contract/evidence Markdown; build thuộc Hoàng, Duy, Giang và Trang. giang/Hải chỉ nhận P2/P3 QA, review, asset trình bày và submission theo Sprint.
15. **H05a contract/state lock** — Kiến trúc tối thiểu: [05-system-architecture.md](../04-engineering/05-system-architecture.md). Case state + care gate: [Process §4](../02-product/03-process.md) (mã API `new_signal`…; cấm `new`/`in_review`/`deferred`/`handed_off`; `defer` = giữ `pending_review` + `review_at`; thiếu `advisor_ref` → dừng assign + mapping-repair). Public ưu tiên = `review_priority_band`, không raw score. BRD/Scope = Target; MVP = PRD.
16. **Persistence MVP** — Chỉ tái sử dụng pattern DWH/ETL/migration của hệ cũ, không copy schema hay row dữ liệu legacy. Hoàng làm `H19` (DB rỗng versioned) sau `H10`; `H20` chỉ nạp fixture M06 đã qua M05b approval, transaction/idempotent và ngoài repo. Thiếu approval → DB không có snapshot và API trả `insufficient_data`.
17. **H10 contract EPU/Data-ML lock** — Chốt từ handoff [M04](10-m04-data-ml-handoff.md); SoT: [EPU](../04-engineering/04-epu-data-integration-contract.md) + [Data-ML/fairness](../04-engineering/08-data-ml-scoring-fairness-contract.md). (a) Taxonomy: `Thôi học`/`Buộc thôi học` → `is_dropout_outcome=true`; `Đang học` → `false`; `Rút học phí`/`Bảo lưu`/khác → `unknown` (loại khỏi mẫu số evaluation; **không** gộp vào positive). Outcome chỉ nội bộ M02/M03. (b) Pseudonym map sinh/lưu **ngoài repo**; quyền giữ: data owner + Admin kỹ thuật/Data-ML; không xuất map sang fixture/API/log/slide. (c) Attendance: tới khi `H15` có approval → toàn nhánh `insufficient_data(attendance_source_unapproved)`; default cửa sổ/mốc tối thiểu trong contract Data-ML §4 (H15 được sửa khi có nguồn). (d) `M05a` Done ≠ nguồn đã duyệt (`M05b`). (e) Contract synthetic cũ superseded → [09…](../04-engineering/09-synthetic-data-ml-fairness-contract-superseded.md).
18. **MVP demo source unlock (M05b + H15, 18/7)** — Không có data-owner ngoài trong cửa sổ demo → **Admin kỹ thuật (Hoàng)** là approver MVP demo. (a) **Semester `M05b`:** duyệt snapshot `v59-empty-program-students` (hash/count từ file local ngoài git); **không** synthetic điểm; **không** commit raw V59/PII/map MSSV — artifact [14-m05b…](14-m05b-semester-approval.md); team dùng package M06 pseudonymous `data/approved/semester/domain_package.json`. (b) **Attendance `H15`:** cho phép **một** nguồn allowlisted `mvp-attendance-over-time` (fixture pseudonymous trong `data/approved/attendance/`); đủ cửa sổ Data-ML §2.2 (≥4 mốc); supersede #9/#12/#13/#17(c) cho path này. (c) Gate vẫn từ chối legacy cấm (`v59-synthetic-students`, profile replacements, marker `"synthetic"` trong payload) — chỉ carve-out nguồn allowlist + `provenance_approved`. (d) Public copy trung lập (điểm + điểm danh theo thời gian); **không** slogan “synthetic”; **không** claim “nhà trường đã duyệt export chính thức”. Mở `M06`.
19. **V08 AI-log timing (18/7)** — Không bắt Hải backfill AI log “ngay”. `V08` **defer** tới cửa sổ gần nộp CP2 / trước `D5`: thu thập log **một thể** (manifest + link online từng thành viên), rồi Hoàng khóa `D5`. CP2 (Live URL + GitHub) **không** phụ thuộc V08.
20. **Advisor-batch mail draft (stretch, 18/7)** — Ban Lãnh đạo lọc case đã duyệt theo `advisor_ref`, hệ thống hỗ trợ **soạn bản nháp mail kèm danh sách** theo từng GV phụ trách. SoT: [11-advisor-batch-mail-draft](../04-engineering/11-advisor-batch-mail-draft.md); PRD **FR-12**. **Draft-only** (Copy/`mailto:`); **cấm** SMTP/auto-send; **không** trả email/SĐT/họ tên trong API public. Tasks: `H21` research → `H22` API → `G06` FE. **Không** block critical path `G02`→`D4b`/CP2.
21. **Agent runtime integration H23–H26 (18/7; provider lịch sử)** — `T02` được công nhận **Done ở mức core/library**, không phải HTTP/runtime FR-08. Runtime dùng bounded DAG do backend điều phối: server suy identity/scope và dựng context từ H02; client chỉ gửi `case_id` + intent/question/locale; stale/insufficient/refused bị chặn trước FPT; raw question/PII không gửi provider; output phải context-bound; Agent không có transition/send/SMTP/Gmail tool. `neutral_draft` chỉ sau approval với mapping hợp lệ. SoT triển khai: [12-agent-runtime-integration-plan](../04-engineering/12-agent-runtime-integration-plan.md); tasks `H23`→`H24`→`H25`→`H26`. Provider target mới xem #22.
22. **OpenAI là provider đích + Global Agent/weekly workflow (owner reconfirm 18/7/2026)** — Phần chọn FPT trong #6/#21 là lịch sử của implementation H23–H26, không còn là hướng provider cho build mới. Runtime đích dùng **OpenAI API** phía backend qua adapter provider-neutral, ưu tiên Responses API + structured output/tool schema; secret chỉ ở environment/secret manager; không có fallback FPT tự động. Việc có `OPENAI_API_KEY` trong `.env`/`.env.example` **đã được wire bởi `H29` Done (18/7 tối)** — Settings/runtime đọc `OPENAI_*`; `get_text_model` chỉ dựng OpenAI hoặc missing-key stand-in; FPT không còn active path. OpenAI không nằm trong weekly data/scoring DAG, không tính delta/priority, không transition case và không send mail. Kiến trúc, mock-feed boundary và backlog: [13-weekly-snapshot-global-agent-architecture](../04-engineering/13-weekly-snapshot-global-agent-architecture.md). Decision #20 draft-only và mọi privacy/care/RBAC gate vẫn giữ nguyên.
23. **H28a readiness lock — weekly build inputs (18/7/2026, owner Hoàng)** — Chốt sáu handle trước schema/runtime weekly (SoT architecture [doc 13](../04-engineering/13-weekly-snapshot-global-agent-architecture.md) §16 + stories [17](17-stories-hoang-weekly-agent.md#h28a)):
    1. **Significant-change / resurfaced:** sau episode terminal, chỉ mở episode mới khi observation cùng `student_ref` có đổi `review_priority_band` **hoặc** tập `contributing_factor` codes (so sánh set equality) so với quan sát cuối trước terminal; `no_longer_detected` **không** auto-resolve/dismiss. Model/threshold/`pseudonym_namespace_version` không comparable → `comparison_unavailable`, không claim “mới”.
    2. **Linked namespace:** chưa có approval handle cho canonical linked bundle cùng pseudonym → **Mode B** (tách nhánh semester vs attendance; cấm fuzzy/heuristic join). Combined feed `H32` chỉ mở khi data owner ghi handle dạng `approval:<dataset_key>:<namespace_version>:<content_hash_prefix>` (không PII/raw path trong repo). Hiện tại: `approval:pending-linked-namespace` — H32 Mode B only.
    3. **Identity/RBAC/retention:** production principal chỉ từ trusted server session/claims: `actor_id`, `active_role` ∈ {`leader`,`advisor`,`admin`}, `org_scope`, `advisor_scope` (nullable trừ role advisor). Client role/localStorage **không** SoT. Receipt/audit metadata giữ tối thiểu **90 ngày**; không lưu raw prompt/answer/draft/PII/`student_ref` plaintext ngoài opaque handle.
    4. **Scheduler:** external **EventBridge → SQS (hoặc approved cron host) → worker** gọi cùng `WeeklyWorkflowService` với CLI; **cấm** APScheduler trong FastAPI web process. Deploy stub: [06-deploy-runbook](../04-engineering/06-deploy-runbook.md) § weekly worker.
    5. **OpenAI model pin:** runtime đọc `OPENAI_API_KEY` + `OPENAI_MODEL` (pin cụ thể sau mocked `H29`; không hardcode “latest”); mọi request `store=false`. Migration claim chỉ sau `H29` Done.
    6. **Email:** giữ Decision #20 draft-only; delivery/SMTP = out of scope (cần PRD/decision riêng nếu sau này mở).
    Mở khóa dependency: `H30`, `H32` (Mode B), `H33b`, `H36`. Combined-feed `H32` vẫn BLOCKED → linked-namespace approval.
24. **Đảo lane Duy↔Giang (18/7 tối)** — **Bùi Khánh Duy** = Frontend (UI, `G06` Notify Copy/`mailto:`, attendance UI, `G07`–`G09`). **Nguyễn Trường Giang** = Data/ML / model / predict (open `M07`/`M08` và hybrid research khi unfreeze). **Hạ Giang** (`giang`) không đổi (UAT/slide). Task ID giữ nguyên; chỉ đổi Owner task mở. Task Done giữ lịch sử người đã làm + ghi chú lane hiện tại theo decision này. SoT board: [Sprint](03-sprint.md). Amend #14 (build vẫn Hoàng+Duy+Giang+Trang; đổi nội dung lane Duy/Giang). FE Notify vẫn draft-only (#20); API `H22` vẫn Hoàng.
