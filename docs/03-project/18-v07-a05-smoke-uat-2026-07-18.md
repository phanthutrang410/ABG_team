# V07 + A05 — Smoke/UAT độc lập (2026-07-18)

> **Thực hiện:** agent thay Văn Hải (`V07`) + Hạ Giang (`A05`) theo yêu cầu owner.
> **Không** sửa Live deploy trong task này; **không** nộp CP2; **không** chạy care transition ghi lên case Live.
> Screenshot UI giữ ngoài repo. Gửi Hoàng để feed `D4r`.

## V07 smoke report — Văn Hải (agent)

```text
## V07 smoke report — Văn Hải
Thời điểm: 2026-07-18 21:29–21:32 +07
Trình duyệt / máy: PowerShell Invoke-WebRequest/RestMethod + Vercel MCP web_fetch (không phải máy deploy owner); không có browser ẩn danh có captcha login
Ẩn danh: một phần (HTTP client / MCP; GitHub API không auth) — SKIP full UI login vì captcha canvas

### URL đã kiểm
- FE EC2: http://52.74.255.88:3000 → PASS có điều kiện — `/` `/login` `/select-role` `/dashboard` 200; `/overview` `/analysis` `/notify` **404** (FE EC2 stale so với Vercel/IA mới)
- API health: http://52.74.255.88:8000/health → PASS — {"status":"ok","service":"silent-shield","database":true}
- GitHub: https://github.com/phanthutrang410/ABG_team → PASS — public=true; README đọc được; tree main không track `.env` (chỉ `.env.example`)
- Vercel candidate: https://abg-team.vercel.app → PASS — `/login` `/overview` `/analysis` `/notify` `/dashboard` 200; same-origin `/health` + `/review-cases` 200 n=50; detail ok

### Luồng sản phẩm
- Login / role: PASS / SKIP interactive — trang login render (meta: “Tín hiệu cần rà soát…”); demo accounts trong source (`quanly`/`gvcn`/`demo` + `demo123`); captcha canvas → không hoàn tất login end-to-end trong smoke này
- List cases: PASS (API) — GET `/review-cases` state=ok n=50; bands uu_tien_som:3 / can_ra_soat:47; data_state partial:50
- Case detail (rc-s-00518c9485a9): PASS (API + Vercel proxy) — band=can_ra_soat; factors grade_*; no model_score/advisor_ref/mssv trong payload list/detail
- Không raw score / không dropout conclusion / không PII: PASS trên API public envelopes; FE meta/description đúng care copy
- Care action (nếu thử): SKIP — cố ý không POST `/cases/{id}/transitions` lên Live shared state

### Defect list (mỗi dòng = 1 bug)
| # | Mức | Bước tái hiện | Kỳ vọng | Thực tế | Screenshot |
|:-:|:--|---|---|---|---|
| 1 | major | Mở `http://52.74.255.88:3000/overview` (và `/analysis`, `/notify`) | FE Live EC2 khớp IA hiện tại / Vercel | 404 Not Found — image FE EC2 cũ hơn candidate | không (HTTP 404) |
| 2 | major | GET `http://52.74.255.88:8000/advisor-handoff-drafts` và qua Vercel rewrite | H22 có trên Live nếu claim Notify/G06 | 404; OpenAPI Live cũng không liệt kê path advisor/weekly/agent turns | không |
| 3 | major | POST `/review-cases/rc-s-00518c9485a9/explanation` body `{"intent":"explain_case","locale":"vi"}` | Agent case-local trả envelope status | 422 Unprocessable Entity (body lỗi rỗng từ client) | không |
| 4 | major (claim) | Đọc detail `rc-s-00518c9485a9` | Attendance MVP đã duyệt phản ánh trên Live | `n_attendance_events=0`, limitations/reason `attendance_source_unapproved`, mọi case `data_state=partial` | không |
| 5 | minor | CORS Origin FE EC2 → API | ACAO khớp FE | PASS `ACAO=http://52.74.255.88:3000` | không |

### Kết luận cho D4r
- [x] Có blocker/major → liệt kê ID defect cần fix trước D4r / trước flip URL nộp
- Defect cần xử lý trước khi coi Live “nộp-ready”: **#1** (đồng bộ FE EC2 hoặc flip Vercel), **#2** nếu claim Notify, **#3** nếu claim Agent case-local trên Live, **#4** nếu claim điểm danh đã vào tín hiệu Live
- V07 status: **PASS WITH MAJORS** (core health + list/detail + GitHub + Vercel candidate xanh; không phải clean PASS không defect)
```

## A05 UAT / claim-copy — Hạ Giang (agent)

| Hạng mục | Kết quả | Ghi chú |
|:--|:--|:--|
| List → case trên Live API | PASS | n=50; detail band + factors; không raw score |
| Loading / error / `insufficient_data` | PASS một phần | Fairness `status=insufficient_data` + `no_approved_audit_attribute` đúng fail-closed; attendance nhánh đang `partial`/`attendance_source_unapproved` — copy phải nói rõ, không claim chuyên cần đã đủ |
| Care copy (ưu tiên rà soát, không dropout) | PASS (source + meta) | `copy.ts` / meta description đúng; dashboard string còn “Dữ liệu demo synthetic” — chấp nhận được nếu slide không overclaim |
| Privacy | PASS API / GAP FE | Public API sạch; **GAP:** `ReportModal` “Xuất báo cáo tổng thể” = `window.print()` bảng nhiều `student_ref` — lệch BRD §9 (bulk identifier). Nên ẩn trước demo nếu còn trên Vercel build |
| Fairness | PASS | Không metric nhóm khi thiếu audit attribute |
| False-alarm / threshold | PASS API | `/config/thresholds` + `/impact` aggregates only (`n_scored`/`n_can_ra_soat`/…) |
| Agent claim | FAIL / hạn chế Live | Explanation 422 trên Live; không claim “Agent đã chạy trên Live” cho tới D4r fix. Case-local UI có trong repo/Vercel bundle nhưng API Live chưa chứng minh |
| Notify / FR-12 | FAIL Live | FE `/notify` có trên Vercel nhưng API advisor 404 — không claim bàn giao mail draft Live |
| Global Agent / weekly | FAIL claim (đúng) | Không có trên OpenAPI Live; **cấm claim** — khớp board “wave chưa ship consumer-ready” |
| Auth/RBAC | GAP đã biết | Demo localStorage + captcha; không production RBAC — claim “phân quyền demo” thôi |
| Interactive care UI | SKIP | Không mutate Live |

### Gap actionables cho `D4r` / `H16`

1. Chốt **URL nộp**: Vercel HTTPS candidate **hoặc** redeploy FE EC2 lên cùng build với `/overview`/`/analysis`/`notify`.
2. Nếu claim Agent: sửa/redeploy explanation path trên Live (hiện 422).
3. Nếu claim Notify/G06: deploy API có `GET /advisor-handoff-drafts` (H22) — hiện thiếu trên Live OpenAPI.
4. Nếu claim điểm danh MVP trên Live: giải thích/fix vì sao mọi case vẫn `attendance_source_unapproved` dù H15/D4b ghi đã import.
5. Ẩn/gỡ bulk “Xuất báo cáo tổng thể” trước demo (BRD §9).
6. Slide/script: chỉ claim health + list/detail band + human review + fairness fail-closed; **không** Global Agent / weekly / production RBAC / hybrid.

### A05 status

**DONE — gaps đã bàn giao.** Không block việc Hoàng bắt đầu triage `D4r`, nhưng **không** đủ để Thu Trang nộp `V05` cho đến khi majors #1 (và các claim liên quan) được xử lý/accepted rõ trên evidence.
