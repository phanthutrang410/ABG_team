services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: silentshield
      POSTGRES_PASSWORD: silentshield
      POSTGRES_DB: silentshield
    ports:
      - "5432:5432"
    volumes:
      - ss_pg:/var/lib/postgresql/data

volumes:
  ss_pg:
