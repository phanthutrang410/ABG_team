# Frontend

Scaffold Next.js ở task G01. UI tiếng Việt; copy hỗ trợ/quan tâm, không kỳ thị.

```powershell
npx create-next-app@latest . --ts --eslint --app --src-dir --tailwind --import-alias "@/*"
```
